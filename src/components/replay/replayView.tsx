"use client";

import { replay } from "@/types/replay/replay";

export default function ReplayView(props: {
    data: replay
}) {
    "use client"

    const data = props.data;

    console.log(data)
    return (
        <div>
            

            
        </div>
    )
}